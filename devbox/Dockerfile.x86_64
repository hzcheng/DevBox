# docker build --no-cache -t hzcheng/ubuntu:22.04.x86_64 -f ./docker/Dockerfile.ubuntu-22.04.x86_64 .
# docker run -it --rm hzcheng/ubuntu:22.04.x86_64 bash

# =============================================================================
# Stage 1: Base image with all development tools
# This stage contains all the heavy dependencies that rarely change
# =============================================================================
FROM mcr.microsoft.com/vscode/devcontainers/cpp:ubuntu-22.04 AS base

# Change source list
RUN mv /etc/apt/sources.list /etc/apt/sources.list.bak && \
    echo "deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy main restricted universe multiverse" > /etc/apt/sources.list && \
    echo "deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse" >> /etc/apt/sources.list && \
    echo "deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse" >> /etc/apt/sources.list 

# Prepare for adding repositories
RUN export DEBIAN_FRONTEND=noninteractive && \
    apt-get update && \
    apt-get install -y \
    software-properties-common ca-certificates wget curl gnupg lsb-release inetutils-ping && \
    # Add LLVM apt repository
    wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key | apt-key add - && \
    add-apt-repository "deb http://apt.llvm.org/focal/ llvm-toolchain-focal-20 main" && \
    # Add Node.js apt repository
    curl -fsSL https://deb.nodesource.com/setup_current.x | bash - && \
    # Add Docker apt repository
    install -m 0755 -d /etc/apt/keyrings && \
    curl -fsSL https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc && \
    chmod a+r /etc/apt/keyrings/docker.asc && \
    echo "Types: deb" > /etc/apt/sources.list.d/docker.sources && \
    echo "URIs: https://download.docker.com/linux/ubuntu" >> /etc/apt/sources.list.d/docker.sources && \
    echo "Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")" >> /etc/apt/sources.list.d/docker.sources && \
    echo "Components: stable" >> /etc/apt/sources.list.d/docker.sources && \
    echo "Signed-By: /etc/apt/keyrings/docker.asc" >> /etc/apt/sources.list.d/docker.sources && \
    # Update package lists
    apt-get update --fix-missing

# Install packages and Chinese locale
RUN apt-get -y install --no-install-recommends \
    tree vim tmux python3-pip tldr patchelf \
    docker-ce-cli docker-compose-plugin docker-buildx-plugin \
    libjansson-dev libsnappy-dev \
    liblzma-dev libz-dev pkg-config libssl-dev graphviz \
    locales \
    && \
    # Set python3 as default
    update-alternatives --install /usr/bin/python python /usr/bin/python3 100 && \
    # Generate Chinese locale
    locale-gen zh_CN.UTF-8 && \
    update-locale LANG=zh_CN.UTF-8

# Install nodejs and npm
RUN apt-get remove -y nodejs && \
    apt-get install -y nodejs && \
    npm install -g npm@latest

# Install clangd-20
RUN apt-get install -y --no-install-recommends clangd-20 && \
    update-alternatives --install /usr/bin/clangd clangd /usr/bin/clangd-20 100

# Install python packages
RUN export DEBIAN_FRONTEND=noninteractive && \
    pip3 install --upgrade pip && \
    pip3 install \
    numpy fabric2 psutil pandas toml distro \
    faker gprof2dot tzlocal matplotlib \
    ipykernel taos-ws-py taospy baostock tushare

COPY ./tmux.conf /root/.tmux.conf
RUN sed -i 's/\r$//' /root/.tmux.conf

RUN apt-get -y install --no-install-recommends \
    autoconf libtool automake autotools-dev

# Install opencode
RUN npm install -g opencode-ai

# Install kimi code
ENV PATH="/root/.local/bin:$PATH"
RUN curl -LsSf https://code.kimi.com/install.sh | bash 

# =============================================================================
# Stage 2: Final image with SSH and code-server
# This stage adds services that may change more frequently
# =============================================================================
FROM base AS devbox

# Install SSH server
RUN apt-get install -y --no-install-recommends openssh-server && \
    sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config && \
    sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config && \
    echo 'root:root' | chpasswd

# Install code-server via .deb package (most reliable for Ubuntu)
RUN apt-get update && \
    curl -fOL https://github.com/coder/code-server/releases/download/v4.96.2/code-server_4.96.2_amd64.deb && \
    dpkg -i code-server_4.96.2_amd64.deb && \
    rm -f code-server_4.96.2_amd64.deb && \
    # Verify installation
    which code-server && code-server --version

# Create code-server config directory
RUN mkdir -p /root/.config/code-server

# Install VS Code extensions from .devcontainer.json
RUN code-server --install-extension llvm-vs-code-extensions.vscode-clangd || true && \
    code-server --install-extension ms-vscode.cmake-tools || true && \
    code-server --install-extension VisualStudioExptTeam.vscodeintellicode || true && \
    code-server --install-extension eamodio.gitlens || true && \
    code-server --install-extension matepek.vscode-catch2-test-adapter || true && \
    code-server --install-extension urosvujosevic.explorer-manager || true && \
    code-server --install-extension mhutchie.git-graph || true && \
    code-server --install-extension ms-python.python || true && \
    code-server --install-extension github.copilot || true && \
    code-server --install-extension twxs.cmake || true && \
    code-server --install-extension github.vscode-pull-request-github || true && \
    code-server --install-extension llvm-vs-code-extensions.vscode-clangd || true && \
    code-server --install-extension liangqin.quick-notes || true && \
    code-server --install-extension ritwickdey.liveserver || true && \
    code-server --install-extension bierner.markdown-checkbox || true && \
    code-server --install-extension yzhang.markdown-all-in-one || true && \
    code-server --install-extension ms-toolsai.jupyter || true && \
    code-server --install-extension natizyskunk.sftp || true && \
    code-server --install-extension streetsidesoftware.code-spell-checker || true && \
    code-server --install-extension ms-python.autopep8 || true && \
    code-server --install-extension ms-azuretools.vscode-docker || true && \
    code-server --install-extension formulahendry.code-runner || true && \
    code-server --install-extension saoudrizwan.claude-dev || true && \
    code-server --install-extension leetcode.vscode-leetcode || true && \
    code-server --install-extension bierner.markdown-preview-github-styles || true && \
    code-server --install-extension bierner.markdown-mermaid || true && \
    code-server --install-extension Kelvin.vscode-sshfs || true

# Copy startup script to a location that won't be overwritten by volume mounts
COPY ./start-services.sh /usr/local/bin/start-services.sh
RUN sed -i 's/\r$//' /usr/local/bin/start-services.sh && chmod +x /usr/local/bin/start-services.sh

# Expose code-server port
EXPOSE 8080 22
