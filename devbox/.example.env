# Volume/Bind Mount 控制
# 对于 dev-root 挂载:
# - 设置为空使用 volume mount（默认）
# - 设置为本地路径如 "./dev_root" 使用 bind mount
DEV_ROOT=

# Volume/Bind Mount 控制
# 对于 dev-projects 挂载:
# - 设置为空使用 volume mount（默认）
# - 设置为本地路径如 "./dev_projects" 使用 bind mount
DEV_PROJECTS=