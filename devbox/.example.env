# Volume/Bind Mount 控制
# 对于 vscode-server 挂载:
# - 设置为空使用 volume mount（默认）
# - 设置为本地路径如 "./vscode-server-data" 使用 bind mount
# VSCODE_SERVER_SOURCE=./vscode-server

# 对于 project-data 挂载:
# - 设置为空使用 volume mount（默认）
# - 设置为本地路径如 "./project-data" 使用 bind mount
# PROJECT_DATA_SOURCE=./project-data